# 1. 현재 동작 방식
```python
for each changed file:
    1. 코드 기반으로 요구사항 형식으로 추출 (코드 -> 자연어)
    2. 자연어와 RAG 유사도 판단
```

# 2. 신규 개발 방식

## 2-1. 개요
0. **준비**
    - 요구사항 명세서를 기반으로 상세 기능 항목을 생성하고, 고유 번호를 부여하여 RAG 벡터 스토어에 저장합니다. [재규]
1. **데이터 수집 (Data Collection)**
    - 1단계 : PR(Pull Request) 변경사항 발생 시, 변경된 파일 목록과 전체 소스 코드를 확보합니다. [수정없음]
2. **코드 분석 및 요구사항 매핑 (Code Analysis & Requirement Mapping)**
    - 2단계 `parse_methods_from_file` : AST 파서를 사용해 각 소스 코드를 메서드 단위로 분해합니다. [수진]
    - 3단계 `summarize_method_function` : 각 메서드의 기능을 설명하는 자연어 요약문을 LLM으로 생성합니다. [수진]
    - 4단계 `match_summary_to_requirement` : 생성된 요약문을 RAG에 검색하여, 각 메서드를 가장 유사한 상세 요구사항 항목과 매칭합니다. [수진]
3. **진척도 종합 및 평가 (Progress Synthesis & Evaluation)**
    - 5단계 `regroup_methods` : 매칭된 상세 요구사항 ID를 기준으로, 관련 메서드 코드들을 그룹화하여 재구성합니다. [종민]
    - 6단계 : 요구사항별로 그룹화된 코드 뭉치를 기반으로, 실제 구현된 기능에 대한 요구사항 명세서를 LLM으로 역생성합니다. [수진]
    - 7단계 : 원본 상세 요구사항(준비 단계)과 역생성된 요구사항 명세서(6단계)를 LLM으로 비교하여 개발 진척도를 최종 평가합니다. [수진]
4. **결과 리포팅 (Result Reporting)**
    - 8단계 : 최종 평가된 진척도 결과를 마크다운 형식으로 정리하여 PR 코멘트로 등록합니다. [수진]


## 2-2. '기능단위분석 RAG' 정의 (요구사항 기반으로 새롭게 추출한 단위 기능 목록)
``` txt
SFR-004-1	수강 중인 과정 목록 조회 
             - 교육일자·교육차수·학습자 등을 고려한 과정 개설 관리
             - 엑셀 업로드를 통한 대량 교육과정 등록
SFR-004-2	완료한 과정 목록 조회
             - ...
SFR-004-3	수강 신청	          
SFR-004-4	수강 신청 취소	          
SFR-004-5	수강 신청 승인 (관리자용)
SFR-004-6	수강 신청 거절 (관리자용)
SFR-004-7	수강 완료 처리 (관리자용)
```

## 2-3. 상세 설계 내용

### 1. 준비 작업 
```python
[재규시작]
(fastapi-server 파일 동작 시작)
- 기능단위분석 RAG 가 존재하는지 확인
- 없는 경우 아래 단계를 통해 생성
- (TO-BE) 요구사항 md 파일이 변경이 인지되면 다시 생성

1. "요구사항에서 검증 항목을 먼저 뽑는다" : 1~7 항목 만들기 
- 개발 구현 단위의 기능으로 항목 작성해달라.
- 요구사항에 있는 소분류 내용을 함께 적어달라.
- 양식은 포맷이 필요함 (미정)

1-1. RAG 구성
- 요구사항의 소분류 하나를 검색 아이템 하나로 유사도 검색할 때 사용될 예정
- 요구사항 번호를 메타데이터로 가지도록
[재규끝]
```

### 2. Webhook 발생
```python
[수진시작]
(Webhook 등 요청 발생)
- 코드 전문 가져오는 부분은 유지

(신규 노드 추가)
for each files : controller / service / impl

    for each method:
        2-1. AST로 메서드 쪼갠다.

    -- 2-1. 결과
    {
        "file_name" : "..../Controller.java"
        "method_list" : [
            {   "method_name" : "root"
                "method_code" : "root() {}"
            },
            {   "method_name" : "apply"
                "method_code" : "apply() {}"
            }
        ]
    }

    2-2. 함수별로 어느 검증 항목에 해당하는지 판단을 한다. (LLM)
    2-2-1. 코드 -> 메서드 단위로 자연어 : "수강신청을 하는 로직"
    2-2-2. 자연어와 RAG 유사도 판단 : SFR-004-1에 해당하는 내용 반환
           - 유사도 판단 대상 : 1번 결과물에 대한 RAG

    -- 2-2. 결과
    {
        "file" : "Controller.java"
        "method_list" : [
            {   "method_name" : "root"
                "method_code" : "root() {}"
                "summary" : "수강신청을 하는 로직" (코드 -> 자연어 바꾼 내용)
                "RFP_number" : [SFR-004-1, .. SFR-004-7]
            },
            {   "method_name" : "apply"
                "method_code" : "apply() {}"
                "summary" : "수강신청을 하는 로직" (코드 -> 자연어 바꾼 내용)
                "RFP_number" : [SFR-004-3]
            }
        ]
    }

2번에 대한 결과
- controller - root()     : [SFR-004-1, .. SFR-004-7]
               apply()    : SFR-004-3
- service - applyClass()  : SFR-004-3
- impl - applyClassImpl() : SFR-004-3

[수진끝]
```

```python
[종민시작]
3. 항목에 맞춰서 코드를 모은 다음에
- 2번 결과 RFP_number 기준으로 (SFR-004-3) 동일 항목에 대한 모든 메서드를 넣는다.
- 각 파일별 전역변수 정보 추가
- 3번 결과
{ RFP-004-01 ... },
{ RFP-004-02 ... },
{ 
  "RFP_name" : "RFP-004-03"
  "file_list" :[
    { 
        path_name : "com......Controller"
        file_name : "Controller.java"
        method_list: [
            { 
                "method_name" : "main"
                "method_code" : "main() {}"
            },
            { 
               "method_name" : "apply"
               "method_code" : "apply() {
                    service.applyClass()
                }"
            }
        ]
        caller_variable : [{
            "path" : com.....Service
            "name" : service  
            }, "Logger logger"
        ]
    },
    {
        path_name : "com......Service"
        file_name : "Service.java"
        ...
    }
  ]
},
{ 005 } ~ { 007 }
[종민끝]
```

```python
[수진시작]
for each 소분류 (SFR-004-1 ~ SFR-004-7) : SFR-004-3

    { "RFP_name" : "RFP-004-03"
      "file_list" :[
        { 
            path_name : "com......Controller"
            file_name : "Controller.java"
            method_list: [
                { 
                    "method_name" : "main"
                    "code" : "main() {}"
                },
                { 
                "method_name" : "apply"
                "code" : "apply() {
                        service.applyClass()
                    }"
                }
            ]
            caller_variable : [{
                "path" : com.....Service
                "name" : service  
                }, "Logger logger"
            ]
        },
        {
            path_name : "com......Service"
            file_name : "Service.java"
            ...
        }
      ]
    }

    4. 코드 기반으로 요구사항 형식으로 추출 (코드 -> 자연어)
        """
        RFP-004-03
        - 입력 :
        - 처리 : 
        - 예외 : 
        - ...
        """
    
    5. 자연어와 RAG 유사도 판단
    - 유사도 판단 대상 : 1번 항목 RAG

    6. 항목별 점수 내기 : "SFR-004-03 에 대한 점수는 0.7/0.8~~ 입니다"

[수진끝]
- PR 코멘트 일단 유지
```

# 3. 현재 문제점
1. 파일이 여러 개 들어오는 경우, 같은 요구사항에 대한 하나의 작업을 파일 별로 분리하여 인식되는 문제 --> 시도해봅시다!

# 4. 해결해야하는 또다른 문제
1. 장기기억 요소가 있어야 한다고 생각함. (메서드 메타데이터, 기능 요약) --> RAG 대상 변경하면서 어느정도 해결됨
2. [TO-BE] 요구사항이 변경되는 경우, 기존 요구사항을 업데이트하는 기능이 필요함
3. [TO-BE] 한번 결과 report 하고나서 수정요청사항에 대해 추가 commit을 반영하는 경우.
4. [TO-Be] Human Feedback 반영 방식
5. [TO-BE] 점수에 대한 판단 근거, 각 판정 분야에 대한 각각의 프롬프트 구성 (세분화)